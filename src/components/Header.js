import React from "react"

const style = {
    horizontalAlign: "center",
    color: "black",
    fontWeight: "bold",
    textAlign: "center"
}

function Header() {

    return (
        <div>
            <header style={style}>
                <h1>
                G✅ADE
                </h1>
            </header>
        </div>
    );
}

export default Header